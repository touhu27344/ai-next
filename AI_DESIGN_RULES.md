---
title: AI_DESIGN_RULES — AIがプロジェクト横断で用いるソフトウェア設計ルールブック
date: 2026-09-20
status: draft-for-human-review
phase: design
author: claude
version: 2.0 (汎用化)
source: docs/research_design_resources_100.md
tags: [design-rules, architecture, ddd, solid, microservices, cqrs, saga, service-mesh, enforcement]
---

# AI_DESIGN_RULES

## 0. メタ・ルール（本書で最優先。読み飛ばし禁止）

**本書は「全部を適用する規則集」ではなく「その都度選ぶための規則集」である。**

第3節以降にはレイヤード、Clean / Hexagonal / Onion、モジュラモノリス、マイクロサービス、
イベント駆動、CQRS、Event Sourcing、Saga、Service Mesh、サーバレス、DDD、GoF 23パターン、
レジリエンスパターン、12 Factor、CI/CD、IaC、GitOps、可観測性まで網羅的に載せている。
**載っていることは、使ってよいことを意味しない。**各様式には「採用してよい条件（採用ゲート）」
が付いており、条件を満たさない採用は本書の違反である。逆に、条件を満たすなら
マイクロサービスもCQRSもSagaも採用してよい。禁止されている様式は存在しない。

| ID | メタ規則 |
| --- | --- |
| M1 | 変更に着手する前に第1節のコンテキスト判定を行い、Tierと要件フラグを**宣言する MUST** |
| M2 | 判定結果に基づき、**採用する章・規則IDの集合を宣言する MUST**。宣言されていない規則は「不採用」として扱う |
| M3 | 検討した主要様式のうち**不採用としたものは理由を1行で記載する MUST**。暗黙の不採用は**認めない** |
| M4 | 採用ゲートを満たさない様式を採用 **MUST NOT**。「将来の拡張性」「一般にベストプラクティスだから」は理由として**認めない** |
| M5 | 判断が割れた場合は**可逆性の高い側**を選ぶ **MUST**。プロセス分割・非同期化・永続化形式の変更は不可逆性が高く、後から戻せない |
| M6 | 規則が衝突した場合の優先順位は ①人間の明示指示 ②プロジェクト固有プロファイル ③本書 ④一般論。**上位が常に勝つ MUST** |
| M7 | プロジェクトごとに `docs/design_rules_profile_<project>.md` を置き、Tier判定・採用集合・**数値閾値**・機械検査対象を記述する **MUST**。本書に数値閾値を直書き **MUST NOT**（本書は可搬、閾値はプロジェクト固有） |
| M8 | 本書を根拠に**既存の動作コードを一括改修 MUST NOT**。規則は新規コードと変更箇所にのみ適用する |
| M9 | Tierが上がった（デプロイ単位が増えた、チームが増えた等）と判断した場合、**コードより先にプロファイルを更新する MUST** |
| M10 | 規則に反する必要が生じたら第11節の逸脱手続きに従う **MUST**。検査の無効化・`xfail` による回避 **MUST NOT** |

強度は3段階。**MUST**（違反はマージ不可）、**MUST NOT**（同）、**SHOULD**（逸脱時に理由を設計文書へ記載）。

## 1. コンテキスト判定（M1の手続き）

### 1.1 判定軸

着手前に次の8軸を評価する。不明な軸は「不明」と書き、**不明を根拠に大きい様式を選ばない**。

| 軸 | 内容 | 判定の目安 |
| --- | --- | --- |
| S: 規模 | コード量・モジュール数 | 数百行 / 数千行 / 数万行以上 |
| U: デプロイ単位 | 独立して再起動・デプロイできる単位の数 | 1 / 2〜3 / 多数 |
| T: 体制 | 同一コードベースを同時に触る人・エージェントの数 | 1 / 数人 / 複数チーム |
| A: 可用性 | 許容できる停止時間 | 自由 / 分単位 / 秒単位・SLO契約あり |
| C: 整合性 | 複数リソースにまたがる業務不変条件の有無 | なし / あり（単一DBで完結） / あり（複数DB） |
| V: 負荷 | 流量・データ量・読み書き比の偏り | 小 / 中 / 読み書きでスケール要求が大きく異なる |
| O: 運用成熟度 | CI・監視・オンコール・常時稼働基盤の有無 | なし / CIのみ / 監視+オンコールあり |
| R: 信頼境界 | 異なる権限・信頼度の主体が接する面の数 | 0〜1 / 2〜3 / 多数・規制対象 |

### 1.2 Tier分類

| Tier | 典型 | 判定条件 |
| --- | --- | --- |
| **T0** 使い捨て | 実験スクリプト、解析ノートブック | 再利用予定なし、S=数百行、他者が保守しない |
| **T1** 単一プロセス | CLI、単一サーバ、ツール、MCPサーバ | U=1, T=1, C=単一DBで完結 |
| **T2** モジュラモノリス | 業務アプリ、複数ドメインを持つ単一デプロイ | U=1 だが内部に複数のドメイン境界、T=数人〜複数チーム |
| **T3** 複数サービス | 2〜数個の独立デプロイ単位 | U≥2、ネットワーク越しの呼び出しがある |
| **T4** 分散プラットフォーム | 多数サービス・複数データストア・SLO運用 | U=多数, A=SLO契約, O=監視+オンコールあり |

**Tierは「望ましい姿」ではなく「現在の事実」で判定する MUST。**
「将来T3になる予定」はT1のプロジェクトをT3として設計する理由に**ならない**（M4）。

### 1.3 章の適用マトリクス

◎=必須、○=条件付き（各章の採用ゲート判定を行う）、－=適用しない。

| 章 | 内容 | T0 | T1 | T2 | T3 | T4 |
| --- | --- | :-: | :-: | :-: | :-: | :-: |
| 2. U系 | 普遍規則（命名・SOLID・DRY等） | ○ | ◎ | ◎ | ◎ | ◎ |
| 3. A系 | アーキテクチャ様式 | － | ○ | ◎ | ◎ | ◎ |
| 4. C系 | データ・整合性 | － | ○ | ○ | ◎ | ◎ |
| 5. D系 | ドメインモデリング（DDD） | － | ○ | ◎ | ◎ | ◎ |
| 6. P系 | デザインパターン | ○ | ○ | ○ | ○ | ○ |
| 7. R系 | レジリエンス | － | ○ | ○ | ◎ | ◎ |
| 8. S系 | セキュリティ | ○ | ◎ | ◎ | ◎ | ◎ |
| 9. O系 | 運用・可観測性・デリバリー | － | ○ | ○ | ◎ | ◎ |
| 10. T系 | プロセスとテスト | ○ | ◎ | ◎ | ◎ | ◎ |

T0であっても U系の一部（命名・秘密情報・危険操作）と S系・T系の最低限は適用する。
**T0を「規則の適用外」の逃げ道として使う MUST NOT。**

## 2. U系: 普遍規則（全Tier）

出典: Clean Code(1), SOLID(3), Refactoring(4), Pragmatic Programmer(5), Code Complete(6),
DRY(15), KISS(16), YAGNI(17), Law of Demeter(18), IoC(19), DI(20)。

| ID | 規則 | 根拠 |
| --- | --- | --- |
| U1 | 名前はドメインの語彙で付け、同義語を新造 **MUST NOT**。1つの概念に1つの語 | ユビキタス言語(53) |
| U2 | 1つのモジュール・クラス・関数は**1つの変更理由**しか持たない **MUST**。閾値（公開メソッド数・行数・引数数）はプロファイルで定める | SRP |
| U3 | 種別による分岐（`isinstance`連鎖・種別文字列の`if/elif`）が閾値を超えたら多態またはStrategyへ置き換える **MUST** | OCP(3,98) |
| U4 | 派生・実装は、宣言されたシグネチャ以外の前提（呼び出し順序、内部状態）を要求 **MUST NOT** | LSP |
| U5 | インタフェース（`Protocol`／抽象基底）は**呼び出し側が実際に使うものだけ**を宣言する **MUST**。将来用メソッドを含め **MUST NOT** | ISP / YAGNI(17) |
| U6 | 依存は引数またはコンストラクタで受け取る **MUST**。モジュールレベルの可変グローバル状態を作成 **MUST NOT** | DIP / IoC(19) |
| U7 | 直接の協力者のみを呼ぶ。`a.b().c().d()` 形の連鎖 **MUST NOT** | Law of Demeter(18) |
| U8 | 同一の判断ロジック・閾値・定数を2箇所以上に複製 **MUST NOT**。正本は1箇所 | DRY(15) |
| U9 | 抽象の導入は**実装が2つ以上存在する時点**まで遅らせる **MUST** | YAGNI(17) / KISS(16) |
| U10 | 到達不能な状態に対するエラー処理・フォールバック・リトライを追加 **MUST NOT** | 最小実装 |
| U11 | 要求に含まれないファイルを変更 **MUST NOT**。整形・リネーム・周辺リファクタを混ぜない | 最小変更 |
| U12 | 自分の変更で不要になった import・変数・関数は削除する **MUST**。既存の未使用コードは**報告のみ**とし削除 **MUST NOT** | 最小変更 |
| U13 | 継承よりコンポジションを **SHOULD**。継承の深さの上限はプロファイルで定める | GoF(21) |
| U14 | 例外は握りつぶさない **MUST**。捕捉するなら**その層で意味のある型へ変換**して再送出する | Clean Code(1) |
| U15 | 秘密情報（トークン、鍵、資格情報）をソース・設定ファイル・ログ・エラー文へ書き **MUST NOT** | 12 Factor(14,56) / OWASP(92) |

## 3. A系: アーキテクチャ様式カタログ

各様式は **［定義］［採用ゲート］［採用時のMUST］［不採用時のMUST］［主な失敗様式］** で記述する。
採用ゲートを満たす様式が複数ある場合、**M5に従い可逆性の高い側**（表の上側）を既定とする。

### A-1 レイヤード / N層

- **定義**: presentation → application → domain → infrastructure の一方向依存。(6)
- **採用ゲート**: T1以上。既定の出発点であり、ゲートは実質「常に可」。
- **採用時のMUST**: 層の一覧と依存方向を設計文書で**宣言する**。逆向きimportを機械検査する（第11節）。
- **失敗様式**: infrastructure の型（ORMエンティティ・HTTPレスポンス）が domain へ漏れる。

### A-2 Clean / Hexagonal（Ports and Adapters）/ Onion

- **定義**: ドメインを中心に置き、外部I/Oをポートとアダプタへ追い出す。依存は常に外→内。(2,57,58)
- **採用ゲート**: 次のいずれかを満たす場合に採用してよい。
  - 外部依存（DB・外部API・ファイル・OSコマンド）が2種類以上ある
  - テストで外部依存を差し替える必要がある
  - ドメインロジックが「入出力を除いても意味を持つ」規模に達している
- **採用時のMUST**:
  - 最内層（型・ドメイン）は外側のパッケージを import **MUST NOT**
  - 外部I/O（ソケット・HTTP・DB・ファイル・`subprocess`）はアダプタ層**のみ**で行う **MUST**
  - アプリケーション層は具象アダプタではなく**インタフェース（`Protocol`）に依存する MUST**
  - 層の追加・新トップレベルパッケージの追加は、**先に設計文書を更新する MUST**
- **不採用時のMUST**: A-1のレイヤードを採用し、依存方向を宣言する。
- **失敗様式**: ポートが実質1実装しかないのに抽象を量産する（U9違反）。

### A-3 モジュラモノリス

- **定義**: 単一デプロイ単位の内部を、公開APIを持つモジュール境界で分割する。
- **採用ゲート**: T2。**複数のドメイン境界**があり、かつ U（デプロイ単位）を増やす理由がまだない場合。
- **採用時のMUST**:
  - モジュール間はモジュールの**公開インタフェース経由でのみ**呼ぶ **MUST**。内部実装への直接import **MUST NOT**
  - モジュールごとにデータ所有を定め、他モジュールのテーブルへ直接アクセス **MUST NOT**
  - モジュール境界を機械検査する **MUST**（import検査）
- **位置づけ**: **マイクロサービス化の前に必ず通過すべき段階 MUST**。境界がモジュールとして機能していないものは、サービスに分けても機能しない。
- **失敗様式**: 「いずれ切り出す」ために作った境界を誰も守らず、共有テーブル経由で結合する。

### A-4 マイクロサービス

- **定義**: 独立デプロイ可能な小さなサービス群。サービスごとにデータを所有する。(44,45,46,78)
- **採用ゲート**: 次を**すべて**満たす場合に限り採用してよい **MUST**。
  1. **組織的理由**がある: 独立して意思決定・デプロイしたいチームが2つ以上実在する
  2. **技術的理由**がある: 部分ごとにスケール特性・可用性要件・技術選択が実際に異なる
  3. **運用能力**がある: CI/CD、集約ログ、分散トレーシング、オンコール体制が**すでに**存在する（O系）
  4. **境界の実績**がある: A-3のモジュール境界が実際に守られている、またはドメイン境界(52)が確定している
  5. **分散の代償を受け入れられる**: ネットワーク障害・部分障害・結果整合性・デバッグ困難化(67)
- **採用時のMUST**:
  - サービスは**自分のデータのみ**を所有する **MUST**。他サービスのDBへ直接アクセス **MUST NOT**
  - サービス間はネットワーク境界であり、**タイムアウト・リトライ上限・障害時の縮退**を必ず定義する **MUST**（R系）
  - 複数サービスにまたがる整合性は C-3（Saga）または結果整合性で扱う **MUST**。分散2相コミットを自作 **MUST NOT**
  - 契約（API/イベントスキーマ）を**バージョン付きで**管理し、契約テストを置く **MUST**（T6）
  - 各サービスの所有者・SLO・依存関係を1枚の表に記載する **MUST**
- **不採用時のMUST**: 不採用理由を**上の5条件のどれを満たさないか**の形で記載する **MUST**（「小さいから」だけでは不可）。
- **失敗様式**: 分散モノリス（同期呼び出しの連鎖で全サービスが同時に落ちる）、共有DB、サービスごとに増える重複認証。

### A-5 イベント駆動アーキテクチャ（EDA）

- **定義**: 生産者がイベントを発行し、消費者が非同期に反応する。(59,68)
- **採用ゲート**: 次のいずれか。
  - 1つの事実に対して**将来増える可能性のある複数の反応**がある
  - 生産者と消費者の可用性・処理速度を**分離する必要がある**
  - 監査・再処理のためにイベント履歴が業務上必要
- **採用時のMUST**:
  - イベントは**過去形の事実**として命名し、命令として使う **MUST NOT**
  - 消費者は**冪等**である **MUST**（同一イベントの重複配信を前提とする）
  - **順序保証の有無**と、順序が崩れた場合の扱いを明記する **MUST**
  - デッドレター（処理不能イベント）の置き場と再投入手順を定義する **MUST**
  - 発行と自DB更新の原子性は **C-4（Outbox）** で担保する **MUST**
- **不採用時のMUST**: 同期呼び出しを採用し、呼び出し失敗時の扱いを明記する。
- **失敗様式**: イベントで隠れた同期依存を作る（結局レスポンスを待っている）、全体の流れを追える文書が存在しない。

### A-6 API Gateway / BFF

- **定義**: クライアントと複数バックエンドの間に置く単一窓口。認証・集約・ルーティングを担う。(50)
- **採用ゲート**: T3以上、かつクライアントが複数バックエンドを直接知る必要が生じている場合。
- **採用時のMUST**: Gatewayに**業務ロジックを置く MUST NOT**（認証・集約・変換・流量制御まで）。Gateway単一障害点のSLOを明記する。
- **失敗様式**: Gatewayが第二のモノリスになる。

### A-7 Service Mesh

- **定義**: サービス間通信（mTLS・再試行・タイムアウト・トレース・流量制御）をサイドカーへ外出しするインフラ層。(55)
- **採用ゲート**: 次を**すべて**満たす場合に限る **MUST**。
  1. T4（サービス数が多く、通信制御をアプリ個別に実装する費用が上回っている）
  2. コンテナオーケストレーション基盤が**すでに運用されている**
  3. メッシュ自体を運用・デバッグできる担当が**実在する**
- **不採用時のMUST**: 通信制御（タイムアウト・リトライ・TLS）をアプリまたは共有ライブラリで実装し、その位置を明記する。
- **失敗様式**: サービス数が一桁なのに導入し、障害時にアプリとメッシュのどちらが原因か切り分けられなくなる。

### A-8 サーバレス / FaaS

- **定義**: イベント起動の短命関数。基盤がスケールと実行を管理する。(60)
- **採用ゲート**: 起動頻度が低く断続的、状態を持たず、起動遅延（コールドスタート）が許容できる処理。
- **採用時のMUST**: 実行時間上限・コールドスタート・ベンダ固有APIへの結合度を明記する **MUST**。状態は外部ストアに置く **MUST**。
- **失敗様式**: 低遅延の同期経路に使う、関数間を同期チェーンで連結する。

### A-9 Strangler Fig（段階的移行）

- **定義**: 既存システムの前段にファサードを置き、機能を1つずつ新実装へ移す。(51)
- **採用ゲート**: 既存の稼働システムを置き換える必要があり、**一括置換のリスクを取れない**場合。
- **採用時のMUST**: 移行の**単位・順序・切り戻し手順・完了条件**を先に定義する **MUST**。新旧が二重に動く期間の**正本（source of truth）を一方に定める MUST**。
- **失敗様式**: 移行が完了せず、新旧が恒久的に併存して両方を保守し続ける。

### A-10 パイプライン / バッチ

- **定義**: 入力→変換の連鎖→出力。段が独立して差し替え可能。
- **採用ゲート**: データ変換が主目的で、段ごとに独立して検証したい場合（研究・解析系に多い）。
- **採用時のMUST**: 各段の入出力を型で定義し、**中間成果物を再現可能な形で永続化する MUST**。再実行時の冪等性を定める **MUST**。

## 4. C系: データと整合性

### C-1 トランザクション境界（全Tier共通の前提）

| ID | 規則 |
| --- | --- |
| C1.1 | 業務不変条件（必ず同時に成立すべき条件）を**列挙する MUST**。どこで守るかを明記する |
| C1.2 | 1つの不変条件は**1つのトランザクション境界**の内側で守る **MUST**。守れない場合はC-3へ |
| C1.3 | 外部呼び出しをトランザクションの内側で行う **MUST NOT**（接続を握ったまま待たない） |
| C1.4 | スキーマ変更は**前方互換の手順**（追加→二重書き→切替→削除）で行う **MUST**。破壊的変更の一発適用 **MUST NOT** |

### C-2 CQRS

- **定義**: 書き込みモデルと読み取りモデルを分離する。(48,77)
- **採用ゲート**: 次のいずれかが**実測で**成立する場合。
  - 読み取りと書き込みのスケール要求・アクセス比が大きく異なる
  - 読み取り要件が書き込みモデルを歪めている（画面都合のフィールドがドメインに侵入している）
  - 読み取り側に独立した最適化（非正規化ビュー・検索インデックス）が必要
- **採用時のMUST**: 読み取りモデルの**鮮度（遅延）を応答に明示する MUST**。読み取りモデルの**再構築手順**を用意する **MUST**。
- **不採用時のMUST**: 単一モデルで読み書きし、読み取り都合のフィールドを**ドメイン型ではなく境界のDTOへ**置く。
- **失敗様式**: CRUDアプリに導入して同期コードを二重化するだけ、結果整合性をユーザに説明できない。

### C-3 Saga（分散トランザクション）

- **定義**: 複数サービスにまたがる更新を、ローカルトランザクションと補償操作の連なりで実現する。(49)
- **採用ゲート**: A-4を採用済みで、かつ C1.2 を単一トランザクションで満たせない不変条件が**実在する**場合。
- **採用時のMUST**:
  - 各ステップに**補償操作を定義する MUST**。補償不能なステップ（外部への不可逆通知・送金確定）は**最後に置く MUST**
  - 各ステップは**冪等 MUST**（再送・再実行に耐える）
  - オーケストレーション型（中央の調整役）かコレオグラフィ型（各サービスが反応）かを**宣言する MUST**。Tier3の初回導入では**オーケストレーション型を SHOULD**（流れが1箇所で読める）
  - 実行中Sagaの状態を**永続化・可視化する MUST**（途中で落ちた取引を人間が追えること）
- **不採用時のMUST**: 不変条件を1サービス内へ寄せる、または結果整合性で許容できるよう**業務要件側を調整する**。

### C-4 Outbox / 冪等性

| ID | 規則 |
| --- | --- |
| C4.1 | 自DB更新と外部への発行を**原子的に**行う必要がある場合、同一トランザクションでoutboxテーブルへ書き、別プロセスが配送する **MUST**（二重書き込み問題の回避） |
| C4.2 | 外部へ送る操作には**冪等キー**を付与する **MUST**。受信側は重複を検出して無害化する **MUST** |
| C4.3 | 「少なくとも1回」配送を前提に設計する **MUST**。「ちょうど1回」を前提に設計 **MUST NOT** |

### C-5 Event Sourcing

- **定義**: 状態ではなく状態変化のイベント列を正本として保存する。(47,77)
- **採用ゲート**: 次を**すべて**満たす場合に限る **MUST**。
  1. 業務上、**全変更履歴と時点再現**が要件である（監査・会計・時系列分析）
  2. イベントスキーマの進化（versioning / upcasting）を運用できる
  3. スナップショット・再構築の運用手順を持てる
- **不採用時のMUST**: 通常の状態保存＋監査ログで要件を満たせることを1行で述べる。
- **失敗様式**: 「履歴があると便利そう」で導入し、スキーマ進化の負債で身動きが取れなくなる。

### C-6 分散データの現実（T3以上で必読）

| ID | 規則 |
| --- | --- |
| C6.1 | データを複製する場合、CAP/PACELC(65,66)の**どちらを捨てるかを宣言する MUST**（分断時に一貫性を捨てるか可用性を捨てるか、通常時に遅延を取るか一貫性を取るか） |
| C6.2 | 分散の8つの誤謬(67)（ネットワークは信頼できる／遅延ゼロ／帯域無限／安全／トポロジ不変／管理者は1人／転送コストゼロ／均質）を**前提にした設計 MUST NOT** |
| C6.3 | 結果整合性を採用した場合、**ユーザ・上位システムから見える不整合の窓**を明記する **MUST** |

## 5. D系: ドメインモデリング（DDD）

戦略的設計はT2以上で**ほぼ常に有効**、戦術的設計は**ゲート付き**。(41,42,43)

### D-1 戦略的設計（T2以上で◎）

| ID | 規則 |
| --- | --- |
| D1.1 | ユビキタス言語(53)を定義し、**コード・設定・DB・文書・UIで同一の語**を使う **MUST**。層をまたぐ言い換え **MUST NOT** |
| D1.2 | 境界づけられたコンテキスト(52)を特定し、**1つのモデルが有効な範囲を宣言する MUST**。全社統一モデルを作ろうとし **MUST NOT** |
| D1.3 | 外部システム・レガシー・上流APIとの接点には**腐敗防止層(54)を置く MUST**。外部の型・フィールド名をそのまま内部へ流し **MUST NOT** |
| D1.4 | コンテキスト間の関係（共有カーネル／顧客-供給者／順応者／公開ホストサービス）を**コンテキストマップとして記載する MUST**（T3以上） |

### D-2 戦術的設計（採用ゲート付き）

| 要素 | 採用ゲート | 採用時のMUST |
| --- | --- | --- |
| 値オブジェクト | ドメインの制約を持つ値（金額・識別子・期間）が存在する | 不変 **MUST**、生成時に検証 **MUST** |
| エンティティ | 同一性が値ではなくIDで決まる対象がある | IDの生成責任を明記 **MUST** |
| 集約(Aggregate) | **1トランザクションで守るべき不変条件が複数オブジェクトにまたがる** | 集約ルート経由でのみ変更 **MUST**、1トランザクション1集約 **SHOULD**、集約間はID参照 **MUST** |
| リポジトリ | 集約の永続化が必要 | 集約単位でのみ入出力 **MUST**、クエリの万能化 **MUST NOT** |
| ドメインイベント | ドメインの出来事に他所が反応する必要がある | 過去形命名 **MUST**、A-5の規則に従う |
| ドメインサービス | どのエンティティにも属さない業務規則がある | 状態を持たない **MUST** |
| ファクトリ | 生成手順が複雑で不変条件の担保が必要 | 生成物が常に有効状態 **MUST** |

**採用しない場合のMUST**: 「貧血モデル（データ構造＋手続き）を意図的に選んだ」と明記する。
小規模・CRUD中心・ロジックが薄い領域では**貧血モデルが正解であり、それは違反ではない**。

## 6. P系: デザインパターン（GoF 23 + α）

パターンは**語彙**であり、**目標ではない**。(21,22,23)

| ID | 規則 |
| --- | --- |
| P1 | パターンは**観測された具体的な問題**に対して導入する **MUST**。導入時は「解決する問題」を1行で記載する **MUST** |
| P2 | 「将来の拡張性」「一般に良いとされる」を唯一の根拠に導入 **MUST NOT**（M4） |
| P3 | パターン名をクラス名に含める場合、**実際にそのパターンである MUST**（`XxxFactory` が生成していない等の不一致 **MUST NOT**） |
| P4 | Singleton(24) を**可変グローバル状態**として使う **MUST NOT**（U6違反）。単一インスタンスが必要なら合成起点で1つだけ作って注入する |
| P5 | DIコンテナ・自動配線の導入ゲート: 手書きの合成コードが**実際に破綻している**こと。T1では手書き合成を **SHOULD** |

**導入トリガ早見表**（トリガが観測されていないパターンの導入はP1違反）

| 分類 | パターン | 導入してよいトリガ |
| --- | --- | --- |
| 生成 | Factory Method(25) / Abstract Factory(26) | 生成する具象が**実際に2つ以上**あり、呼び出し側から隠す必要がある |
| 生成 | Builder(27) | 省略可能パラメータが多く、段階的構築か不変オブジェクト生成が必要 |
| 生成 | Prototype(28) | 生成費用が高く、既存インスタンスの複製が自然 |
| 構造 | Adapter(29) | 外部の型を内部の型へ変換する必要がある（＝腐敗防止層の実装手段） |
| 構造 | Bridge(30) | 抽象と実装が**独立に**増える2軸がある |
| 構造 | Composite(31) | 木構造を「全体と部分」で同一視して扱う必要がある |
| 構造 | Decorator(32) | 既存の振る舞いに**直交する関心事**（ログ・キャッシュ・計測）を重ねる |
| 構造 | Facade(33) | 複数の下位APIを1つの操作として外へ見せたい |
| 構造 | Flyweight(34) | 同一データの大量インスタンスがメモリを実測で圧迫している |
| 構造 | Proxy(35) | アクセス制御・遅延生成・リモート呼び出しの介在が必要 |
| 振る舞い | Chain of Responsibility(36) | 複数の処理候補を順に試す構造が実在する（ミドルウェア等） |
| 振る舞い | Command(37) | 操作の記録・遅延実行・取り消し・キュー投入が必要 |
| 振る舞い | Iterator(38) | 走査方法を集合の内部構造から分離する必要がある |
| 振る舞い | Mediator(39) | 多対多の直接参照が実際に絡まっている |
| 振る舞い | Memento(40) | 状態の保存と復元（取り消し・チェックポイント）が必要 |
| 振る舞い | State(97) | 状態ごとに振る舞いが変わり、遷移表が明確に書ける |
| 振る舞い | Strategy(98) | 交換可能なアルゴリズムが**2つ以上実在する**（U3の置き換え先） |
| 振る舞い | Template Method(99) | 手順の骨格が同一で一部だけ異なる実装が2つ以上ある |
| 振る舞い | Visitor(100) | データ構造が安定していて、**操作だけが増える**ことが分かっている |

## 7. R系: レジリエンス（T1は○、T3以上は◎）

| ID | 規則 | ゲート |
| --- | --- | --- |
| R1 | すべての外部呼び出し（ネットワーク・DB・サブプロセス）に**タイムアウトを設定する MUST** | 全Tier |
| R2 | リトライ(74)は**冪等な操作にのみ**適用する **MUST**。試行回数上限とジッタ付きバックオフを設定する **MUST**。無制限リトライ **MUST NOT** | 全Tier |
| R3 | Circuit Breaker(72) の採用ゲート: 上流が**複数**あるか、上流障害時の再試行が**自システムを悪化させている**ことが観測された場合。単一上流＋タイムアウトで足りる場合は不採用 **MUST** | T3以上 |
| R4 | Bulkhead(73) の採用ゲート: 1つの依存先の遅延が**他の処理を巻き込んだ実績**がある（共有スレッドプール/接続プールの枯渇） | T3以上 |
| R5 | Rate Limiting(75) / Throttling(76) の採用ゲート: **信頼できない呼び出し元**が存在するか、上流に契約上の流量制限がある | T2以上 |
| R6 | 応答サイズ・処理件数・実行時間に**上限（予算）を設ける MUST**。超過時は**縮退したことを応答に明示する MUST**（黙って打ち切り **MUST NOT**） | 全Tier |
| R7 | 障害時の縮退動作（失敗を返す／キャッシュを返す／部分結果を返す）を**宣言する MUST**。暗黙のフォールバック **MUST NOT** | T1以上 |
| R8 | ヘルスチェックは**依存先の状態を反映する MUST**（プロセス生存だけを返す「嘘のhealthy」**MUST NOT**） | T3以上 |
| R9 | Chaos Engineering(91) の採用ゲート: 本番相当環境・監視・切り戻し手順が**すべて揃っている**こと | T4 |

## 8. S系: セキュリティ（全Tierで◎）

出典: OWASP Top Ten(92), Threat Modeling(93)。

| ID | 規則 |
| --- | --- |
| S1 | **信頼境界を列挙する MUST**（誰が・どの権限で・どのデータに触れるか）。境界を跨ぐ入力は**すべて信用しない** |
| S2 | 入力検証は**境界で1回、明示的に**行う **MUST**。型・長さ・文字種・範囲・列挙値を宣言する **MUST** |
| S3 | 外部入力から**パス・URL・SQL・シェル語・コードを直接構築 MUST NOT**。パラメータ化・許可リスト・サーバ側生成を使う |
| S4 | 認可判定は**1箇所（アプリケーション層の入口）で最初に**行う **MUST**。層ごとに分散 **MUST NOT** |
| S5 | 秘密情報は環境変数または秘密管理機構から取得する **MUST**（U15）。ログ・エラー・テレメトリへの出力 **MUST NOT** |
| S6 | 例外文言・スタックトレース・内部パス・上流URLを外部応答に含め **MUST NOT**。定型のエラーコードのみ返す |
| S7 | LLM・外部サイト・ユーザ投稿など**信用できない生成物を実行・信頼 MUST NOT**。応答に含める場合は「外部由来・未検証」を**データとして明示する MUST**（プロンプトインジェクション対策） |
| S8 | 破壊的操作（削除・上書き・外部公開・送信）は**人間の明示的な承認を必要とする MUST**。AIエージェントの既定権限に含め **MUST NOT** |
| S9 | 依存パッケージは版を固定し、追加時に**出所と必要性を記載する MUST** |
| S10 | ネットワーク到達先は**設定の許可リストに限定する MUST**。ホスト・IPをコードへ直書き **MUST NOT** |
| S11 | 新しい信頼境界を追加する変更では、**脅威モデル（何が起きうるか・どう防ぐか）を設計文書に1段落記載する MUST** |

## 9. O系: 運用・可観測性・デリバリー

**O系の規則は「運用する主体が実在する」ことを前提とする。**
運用者のいない仕組みを追加 **MUST NOT**（誰も見ないダッシュボード、誰も直さない失敗するCI）。

| ID | 規則 | ゲート |
| --- | --- | --- |
| O1 | 設定は環境から注入し、コードと分離する **MUST**（12 Factor III）(14,56) | T1以上 |
| O2 | ログは標準出力へのイベントストリームとして出す **MUST**。ログファイルの自前ローテーション実装 **MUST NOT** | T1以上 |
| O3 | ログは構造化し、**相関ID**を含める **MUST** | T2以上 |
| O4 | プロセスはステートレスにし、状態は外部ストアへ **MUST**（12 Factor VI） | T3以上 |
| O5 | CI（自動テスト実行）を用意する **MUST**(87)。失敗したCIを放置 **MUST NOT** | T1以上 |
| O6 | デプロイ手順は**再現可能な形で記述する MUST**。手作業の暗黙手順 **MUST NOT**(88) | T2以上 |
| O7 | 可観測性(94): **メトリクス・ログ・トレース**のうち、何を取り何を取らないかを宣言する **MUST** | T2以上 |
| O8 | OpenTelemetry(95) の採用ゲート: サービスを跨ぐ因果を追う必要が**実際に生じた**とき。単一プロセスでは不採用 **MUST** | T3以上 |
| O9 | IaC(89) / GitOps(90) の採用ゲート: 管理対象インフラが**複数環境または複数台**あり、Gitを正本にする運用を維持できること | T3以上 |
| O10 | SLI/SLO(69) を定義してから可用性向上の作業に着手する **MUST**。数値目標なしの「安定化」作業 **MUST NOT** | T4 |
| O11 | 常時稼働ホスト・監視・オンコールが**未整備の段階で**、それを前提とする様式（A-4, A-7, O8〜O10）を採用 **MUST NOT** | 全Tier |

## 10. T系: 開発プロセスとテスト（全Tierで◎）

出典: TDD(84), BDD(85), XP(83), Legacy Code(7), Google Testing(13)。

| ID | 規則 |
| --- | --- |
| T1 | 着手前に**検証可能な合格条件**を宣言する **MUST**（通すべきテスト名、または確認するinvariant）。TDDの厳密なred-green-refactorは**要求しない** |
| T2 | 振る舞いを変える変更は、その振る舞いを固定するテストを**同一コミットに含める MUST** |
| T3 | 変更後にテスト一式を実行し、**実出力を報告する MUST**。未実行を「通った」と報告 **MUST NOT** |
| T4 | テストは**境界値**（上限・下限・超過・空・不正型）を含める **MUST**。正常系のみで完了とし **MUST NOT** |
| T5 | 単体テストで実ネットワーク・実DB・実外部サービスへ接続 **MUST NOT**。インタフェースのフェイク実装を使う |
| T6 | 契約テスト（供給者と消費者の合意）を置く **MUST** | T3以上 |
| T7 | レガシーコードを変更する場合、**先に現在の振る舞いを固定するテスト**（characterization test）を書く **MUST**(7) |
| T8 | BDD(85) の採用ゲート: 非開発者が受入条件を読む・書く**実際の運用がある**こと。形式だけのGherkin **MUST NOT** |
| T9 | テストの比率（単体 : 結合 : E2E）と各層の責務をプロファイルで宣言する **MUST** |
| T10 | 設計文書に書かれていない機能を実装 **MUST NOT**。必要と判断したら**設計を先に更新する**（Design-First） |

## 11. V系: 適合検査と逸脱手続き

### 11.1 機械検査の義務

| ID | 規則 |
| --- | --- |
| V1 | プロファイルで**採用した規則のうち機械検査可能なものは、検査を実装する MUST**。レビュー任せにし **MUST NOT** |
| V2 | 機械検査の対象と判定方法を、プロファイルの表に列挙する **MUST** |
| V3 | 検査が既存コードで失敗した場合、**既定はコード修正**とする **MUST**。規則の緩和は11.2の逸脱手続きを経る **MUST** |

**機械検査しやすい規則の型**（AST／静的解析で判定可能）:

| 検査の型 | 対象になりうる規則 |
| --- | --- |
| import方向の検査 | A-1, A-2, A-3 の層・モジュール境界 |
| 特定モジュールのimport局在 | A-2（I/Oのアダプタ層局在）、S3（`subprocess`/`eval`の禁止） |
| 基底クラス・デコレータの強制 | 型定義の厳格化、境界DTOの規約 |
| リテラル検査 | S5（秘密情報）、S10（IP・ホスト直書き） |
| 構文メトリクス | U2（公開メソッド数・関数行数・引数数）、U13（継承の深さ） |

**機械検査に向かない規則**（レビュー項目に留める）: U7（Demeter）、D系の妥当性、各様式の採用ゲート判定。

### 11.2 逸脱手続き

規則に反する変更が必要な場合、次の3点を**同一コミットのメッセージ**に記載する **MUST**。

1. 逸脱する規則ID
2. 逸脱しなければ達成できない理由
3. 逸脱の影響範囲と、解消の条件（解消予定がない場合は「なし」と明記）

テストの無効化・`xfail`・検査の除外リストによる回避 **MUST NOT**（M10）。
恒久的な例外はプロファイルの「既知の逸脱」節へ記載する **MUST**。

## 12. プロジェクト固有プロファイルの雛形（M7）

`docs/design_rules_profile_<project>.md` に次の見出しを置く。**すべて必須**。

```markdown
1. コンテキスト判定    8軸の評価結果と Tier、判定日
2. 採用する章と規則ID  適用マトリクスに対する実際の採用集合
3. 不採用の宣言        検討した主要様式と、不採用理由（各1行）
4. 数値閾値            U2 等の具体値と、その値にした根拠
5. ディレクトリ束縛    本書の抽象的な層名を、実際のパスへ対応付ける表
6. 機械検査の一覧      検査名・対象規則ID・判定方法・実装ファイル
7. 既知の逸脱          規則ID・理由・影響・解消条件
8. 再判定の記録        Tierや採用集合を変更した日と理由（M9）
```

## 13. 100件リソースの対応表

| 資料番号 | 本書での扱い |
| --- | --- |
| 1,4,5,6,7,10,11,12,13 | 第2節 U系・第10節 T系の背景。個別規則化はしない |
| 2,57,58 | A-2（Clean / Hexagonal / Onion） |
| 3 | U2〜U6（SOLID） |
| 8,9,21,22,23,24〜40,97〜100 | 第6節 P系（導入トリガ表） |
| 14,56 | O1, O2, O4（12 Factor） |
| 15,16,17,18,19,20 | U6〜U9, P5 |
| 41,42,43,52,53,54 | 第5節 D系（DDD） |
| 44,45,46,78 | A-4（マイクロサービス） |
| 47,77 | C-5（Event Sourcing） |
| 48 | C-2（CQRS） |
| 49 | C-3（Saga） |
| 50 | A-6（API Gateway / BFF） |
| 51 | A-9（Strangler Fig） |
| 55 | A-7（Service Mesh） |
| 59,68 | A-5（EDA / Reactive） |
| 60 | A-8（サーバレス） |
| 61,70,71,79,80 | 判断材料。規則化はしない（参照先として維持） |
| 62,63,64,96 | O9（IaC / クラウド設計）の参照資料 |
| 65,66,67 | C6.1〜C6.3 |
| 69 | O10（SLI/SLO） |
| 72,73,74,75,76 | R3, R4, R2, R5 |
| 81,82,83 | 進め方の背景。本書は開発プロセスの形式を規定しない |
| 84,85 | T1, T8 |
| 86,87,88 | O5, O6 |
| 89,90 | O9 |
| 91 | R9 |
| 92,93 | 第8節 S系 |
| 94,95 | O7, O8 |

## 14. 未解決事項

1. **本書は1ファイルで約400行あり、`AI_RULES.md` の「1文書80〜200行」の目安を超える。**
   規則集としての一覧性（第0節のメタ規則と各章のゲートを続けて読めること）を優先した。
   章ごとの分割は、実際に参照しづらくなった時点で行う。
2. **U2・U13の数値閾値を本書から削除した（M7）。** プロファイル未作成のプロジェクトでは
   これらの規則は**判定不能**になる。プロファイルの作成を先行させること。
3. **採用ゲートの判定は現時点で人間またはAIの主観に依存する。** ゲート判定を記録する
   フォーマット（プロファイル第1〜3節）は定めたが、判定の妥当性を検査する手段はない。
4. **T0（使い捨て）の扱いは緩い。** 使い捨てのつもりのコードが残存してT1になる経路への
   歯止めは、M9（Tier再判定）のみであり、強制力は弱い。

## 15. 次のアクション

1. 人間が本書を査読し、第0節のメタ規則と第1節のTier定義を承認または修正する。
2. 承認後、`docs/design_rules_profile_lab_mcp_system.md`（本リポジトリのプロファイル）を
   本書の構成に合わせて確定する。v1.0の規則ID（A1〜F5）はそちらへ移設済み。
3. プロファイル第6節の表に従い `tests/test_design_rules.py` を実装する（IMPLEMENTフェーズ）。
4. 他プロジェクトへ本書を適用する際は、**本書を複製せずリンクし**、プロファイルのみを新設する。

## 参照

- `docs/research_design_resources_100.md` — 本書の入力（100件のリソース一覧）
- `docs/design_rules_profile_lab_mcp_system.md` — 本リポジトリ向けプロファイル（数値閾値・機械検査・逸脱）
- `docs/design_lab_mcp_grand_architecture.md` — 本リポジトリの層構造・信頼境界の正本
- `/home/tanida/AI_RULES.md` — 上位の安全・最小変更ルール（本書に優先する）
